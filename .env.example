# BotMatrix Environment Variables

# Database Configuration
# Database type: postgres or sqlite (default: postgres)
DB_TYPE=postgres
# PostgreSQL connection settings
DB_HOST=localhost
DB_PORT=5432
DB_USER=botmatrix
DB_PASSWORD=your_password
DB_NAME=botmatrix_db
DB_SSL_MODE=disable
# SQLite database file path (used when DB_TYPE=sqlite)
DB_PATH=./botmatrix.db

# Redis Configuration
REDIS_ADDR=192.168.0.126:6379
REDIS_PWD=redis_zsYik8

# BotNexus Configuration
WS_PORT=:3001
WEBUI_PORT=:5000

# WxBot Configuration
# ID of the bot itself (integer)
BOT_SELF_ID=1098299491
